docx==0.2.4               # For handling Word documents
extract_msg==0.48.5       # For parsing .msg files
Flask==3.1.0              # Lightweight web framework
openai==1.59.6            # For OpenAI API interaction
pandas==2.2.3             # For data manipulation
python-dotenv==1.0.1      # For environment variable management
python_docx==1.1.2        # Alternative library for Word documents
requests==2.32.3          # For HTTP requests

aiohttp==3.7.4.post0      # Compatible version to avoid conflicts
fastapi==0.95.2           # Works with older aiohttp versions
pydantic==1.10.8          # Data validation for FastAPI
gunicorn==21.0.1          # For production server
uvicorn==0.22.0           # ASGI server compatible with FastAPI
